diff --git a/node_modules/react-native-star-rating-widget/lib/commonjs/StarIcon.js b/node_modules/react-native-star-rating-widget/lib/commonjs/StarIcon.js
index fdb27f1..783cca9 100644
--- a/node_modules/react-native-star-rating-widget/lib/commonjs/StarIcon.js
+++ b/node_modules/react-native-star-rating-widget/lib/commonjs/StarIcon.js
@@ -158,6 +158,42 @@ const StarHalf = _ref5 => {
     strokeWidth: "1.5"
   }));
 };
+const StarFraction = _ref6 => {
+  let {
+    size,
+    color,
+    index,
+    fill = 0
+  } = _ref6;
+  const clampedFill = Math.min(1, Math.max(0, fill));
+  const gradientId = `fraction-gradient-${index}`;
+  const offset = `${Math.round(clampedFill * 100)}%`;
+  return /*#__PURE__*/_react.default.createElement(_reactNativeSvg.default, {
+    height: size,
+    viewBox: "0 0 24 24",
+    width: size,
+    style: _reactNative.I18nManager.isRTL ? RTL_TRANSFORM : undefined
+  }, /*#__PURE__*/_react.default.createElement(_reactNativeSvg.Defs, null, /*#__PURE__*/_react.default.createElement(_reactNativeSvg.LinearGradient, {
+    id: gradientId,
+    x1: "0",
+    y1: "0",
+    x2: "1",
+    y2: "0"
+  }, /*#__PURE__*/_react.default.createElement(_reactNativeSvg.Stop, {
+    offset: offset,
+    stopColor: color,
+    stopOpacity: "1"
+  }), /*#__PURE__*/_react.default.createElement(_reactNativeSvg.Stop, {
+    offset: offset,
+    stopColor: color,
+    stopOpacity: "0"
+  }))), /*#__PURE__*/_react.default.createElement(_reactNativeSvg.Path, {
+    d: "M12 17.27L18.18 21l-1.64-7.03L22 9.24l-7.19-.61L12 2 9.19 8.63 2 9.24l5.46 4.73L5.82 21z",
+    fill: `url(#${gradientId})`,
+    stroke: color,
+    strokeWidth: "1.5"
+  }));
+};
 const getStarComponent = type => {
   switch (type) {
     case 'full':
@@ -168,22 +204,26 @@ const getStarComponent = type => {
       return StarQuarter;
     case 'three-quarter':
       return StarThreeQuarter;
+    case 'fraction':
+      return StarFraction;
     default:
       return StarBorder;
   }
 };
-const StarIcon = _ref6 => {
+const StarIcon = _ref7 => {
   let {
     index,
     type,
     size,
-    color
-  } = _ref6;
+    color,
+    fill
+  } = _ref7;
   const Component = getStarComponent(type);
   return /*#__PURE__*/_react.default.createElement(Component, {
     index: index,
     size: size,
-    color: color
+    color: color,
+    fill: fill
   });
 };
 var _default = StarIcon;
diff --git a/node_modules/react-native-star-rating-widget/lib/commonjs/StarRating.js b/node_modules/react-native-star-rating-widget/lib/commonjs/StarRating.js
index 9feee92..28fbe0f 100644
--- a/node_modules/react-native-star-rating-widget/lib/commonjs/StarRating.js
+++ b/node_modules/react-native-star-rating-widget/lib/commonjs/StarRating.js
@@ -17,6 +17,12 @@ const defaultAnimationConfig = {
   scale: 1.2,
   delay: 300
 };
+const getStepValue = step => {
+  if (typeof step === 'number') return step;
+  if (step === 'quarter') return 0.25;
+  if (step === 'half') return 0.5;
+  return 1;
+};
 const StarRating = _ref => {
   let {
     rating,
@@ -40,7 +46,9 @@ const StarRating = _ref => {
     accessabilityActivateLabel = 'activate (default)',
     accessibilityAdjustmentLabel = '%value% stars'
   } = _ref;
-  const multiplier = step === 'quarter' ? 4 : step === 'half' ? 2 : 1;
+  const stepValue = Math.max(0.01, getStepValue(step));
+  const multiplier = 1 / stepValue;
+  const activeThreshold = stepValue;
   const width = _react.default.useRef();
   const [isInteracting, setInteracting] = _react.default.useState(false);
   const [stagedRating, setStagedRating] = _react.default.useState(rating);
@@ -51,7 +59,9 @@ const StarRating = _ref => {
       if (isRTL) {
         return calculateRating(width.current - x, false);
       }
-      const newRating = step !== 'full' ? Math.max(0, Math.min(Math.round(x / width.current * maxStars * multiplier + 0.2) / multiplier, maxStars)) : Math.ceil(x / width.current * maxStars);
+      const rawRating = x / width.current * maxStars;
+      const steppedRating = stepValue >= 1 ? Math.ceil(rawRating) : Math.round(rawRating * multiplier) / multiplier;
+      const newRating = Math.max(0, Math.min(steppedRating, maxStars));
       return newRating;
     };
     const handleChange = newRating => {
@@ -120,7 +130,7 @@ const StarRating = _ref => {
       label: accessabilityActivateLabel
     }],
     onAccessibilityAction: event => {
-      const incrementor = step === 'half' ? 0.5 : step === 'quarter' ? 0.25 : 1;
+      const incrementor = stepValue;
       switch (event.nativeEvent.actionName) {
         case 'increment':
           if (stagedRating >= maxStars) {
@@ -143,17 +153,19 @@ const StarRating = _ref => {
           break;
       }
     }
-  }), (0, _utils.getStars)(rating, maxStars, step).map((starType, i) => {
+  }), (0, _utils.getStars)(rating, maxStars, step).map((star, i) => {
+    const isEmpty = star.type === 'empty';
     return /*#__PURE__*/_react.default.createElement(AnimatedIcon, {
       key: i,
-      active: isInteracting && rating - i >= 0.5,
+      active: isInteracting && rating - i >= activeThreshold,
       animationConfig: animationConfig,
       style: starStyle
     }, /*#__PURE__*/_react.default.createElement(StarIconComponent, {
       index: i,
-      type: starType,
+      type: star.type,
+      fill: star.fill,
       size: starSize,
-      color: starType === 'empty' ? emptyColor : color
+      color: isEmpty ? emptyColor : color
     }));
   })));
 };
diff --git a/node_modules/react-native-star-rating-widget/lib/commonjs/StarRatingDisplay.js b/node_modules/react-native-star-rating-widget/lib/commonjs/StarRatingDisplay.js
index ed7fe2a..a774ca0 100644
--- a/node_modules/react-native-star-rating-widget/lib/commonjs/StarRatingDisplay.js
+++ b/node_modules/react-native-star-rating-widget/lib/commonjs/StarRatingDisplay.js
@@ -28,15 +28,17 @@ const StarRatingDisplay = _ref => {
     style: [styles.starRating, style],
     accessibilityLabel: accessibilityLabel,
     testID: testID
-  }, (0, _utils.getStars)(rating, maxStars, step).map((starType, i) => {
+  }, (0, _utils.getStars)(rating, maxStars, step).map((star, i) => {
+    const isEmpty = star.type === 'empty';
     return /*#__PURE__*/_react.default.createElement(_reactNative.View, {
       key: i,
       style: [styles.star, starStyle]
     }, /*#__PURE__*/_react.default.createElement(StarIconComponent, {
       index: i,
-      type: starType,
+      type: star.type,
+      fill: star.fill,
       size: starSize,
-      color: starType === 'empty' ? emptyColor : color
+      color: isEmpty ? emptyColor : color
     }));
   }));
 };
diff --git a/node_modules/react-native-star-rating-widget/lib/commonjs/utils.js b/node_modules/react-native-star-rating-widget/lib/commonjs/utils.js
index e705cb9..7d16f66 100644
--- a/node_modules/react-native-star-rating-widget/lib/commonjs/utils.js
+++ b/node_modules/react-native-star-rating-widget/lib/commonjs/utils.js
@@ -4,18 +4,42 @@ Object.defineProperty(exports, "__esModule", {
   value: true
 });
 exports.getStars = getStars;
+const clamp = (value, min, max) => Math.min(max, Math.max(min, value));
 function getStars(rating, maxStars, step) {
   return [...Array(maxStars)].map((_, i) => {
     const remainder = rating - i;
-    if (remainder >= 1) return 'full';
+    if (remainder >= 1) return {
+      type: 'full'
+    };
+    if (typeof step === 'number') {
+      if (remainder > 0) {
+        return {
+          type: 'fraction',
+          fill: clamp(remainder, 0, 1)
+        };
+      }
+      return {
+        type: 'empty'
+      };
+    }
     if (step === 'quarter') {
-      if (remainder >= 0.75) return 'three-quarter';
-      if (remainder >= 0.5) return 'half';
-      if (remainder >= 0.25) return 'quarter';
+      if (remainder >= 0.75) return {
+        type: 'three-quarter'
+      };
+      if (remainder >= 0.5) return {
+        type: 'half'
+      };
+      if (remainder >= 0.25) return {
+        type: 'quarter'
+      };
     } else if (step === 'half') {
-      if (remainder >= 0.5) return 'half';
+      if (remainder >= 0.5) return {
+        type: 'half'
+      };
     }
-    return 'empty';
+    return {
+      type: 'empty'
+    };
   });
 }
 //# sourceMappingURL=utils.js.map
diff --git a/node_modules/react-native-star-rating-widget/lib/module/StarIcon.js b/node_modules/react-native-star-rating-widget/lib/module/StarIcon.js
index 571abbc..512e5db 100644
--- a/node_modules/react-native-star-rating-widget/lib/module/StarIcon.js
+++ b/node_modules/react-native-star-rating-widget/lib/module/StarIcon.js
@@ -149,6 +149,42 @@ const StarHalf = _ref5 => {
     strokeWidth: "1.5"
   }));
 };
+const StarFraction = _ref6 => {
+  let {
+    size,
+    color,
+    index,
+    fill = 0
+  } = _ref6;
+  const clampedFill = Math.min(1, Math.max(0, fill));
+  const gradientId = `fraction-gradient-${index}`;
+  const offset = `${Math.round(clampedFill * 100)}%`;
+  return /*#__PURE__*/React.createElement(Svg, {
+    height: size,
+    viewBox: "0 0 24 24",
+    width: size,
+    style: I18nManager.isRTL ? RTL_TRANSFORM : undefined
+  }, /*#__PURE__*/React.createElement(Defs, null, /*#__PURE__*/React.createElement(LinearGradient, {
+    id: gradientId,
+    x1: "0",
+    y1: "0",
+    x2: "1",
+    y2: "0"
+  }, /*#__PURE__*/React.createElement(Stop, {
+    offset: offset,
+    stopColor: color,
+    stopOpacity: "1"
+  }), /*#__PURE__*/React.createElement(Stop, {
+    offset: offset,
+    stopColor: color,
+    stopOpacity: "0"
+  }))), /*#__PURE__*/React.createElement(Path, {
+    d: "M12 17.27L18.18 21l-1.64-7.03L22 9.24l-7.19-.61L12 2 9.19 8.63 2 9.24l5.46 4.73L5.82 21z",
+    fill: `url(#${gradientId})`,
+    stroke: color,
+    strokeWidth: "1.5"
+  }));
+};
 const getStarComponent = type => {
   switch (type) {
     case 'full':
@@ -159,22 +195,26 @@ const getStarComponent = type => {
       return StarQuarter;
     case 'three-quarter':
       return StarThreeQuarter;
+    case 'fraction':
+      return StarFraction;
     default:
       return StarBorder;
   }
 };
-const StarIcon = _ref6 => {
+const StarIcon = _ref7 => {
   let {
     index,
     type,
     size,
-    color
-  } = _ref6;
+    color,
+    fill
+  } = _ref7;
   const Component = getStarComponent(type);
   return /*#__PURE__*/React.createElement(Component, {
     index: index,
     size: size,
-    color: color
+    color: color,
+    fill: fill
   });
 };
 export default StarIcon;
diff --git a/node_modules/react-native-star-rating-widget/lib/module/StarRating.js b/node_modules/react-native-star-rating-widget/lib/module/StarRating.js
index d88ae2c..35e3aa7 100644
--- a/node_modules/react-native-star-rating-widget/lib/module/StarRating.js
+++ b/node_modules/react-native-star-rating-widget/lib/module/StarRating.js
@@ -10,6 +10,12 @@ const defaultAnimationConfig = {
   scale: 1.2,
   delay: 300
 };
+const getStepValue = step => {
+  if (typeof step === 'number') return step;
+  if (step === 'quarter') return 0.25;
+  if (step === 'half') return 0.5;
+  return 1;
+};
 const StarRating = _ref => {
   let {
     rating,
@@ -33,7 +39,9 @@ const StarRating = _ref => {
     accessabilityActivateLabel = 'activate (default)',
     accessibilityAdjustmentLabel = '%value% stars'
   } = _ref;
-  const multiplier = step === 'quarter' ? 4 : step === 'half' ? 2 : 1;
+  const stepValue = Math.max(0.01, getStepValue(step));
+  const multiplier = 1 / stepValue;
+  const activeThreshold = stepValue;
   const width = React.useRef();
   const [isInteracting, setInteracting] = React.useState(false);
   const [stagedRating, setStagedRating] = React.useState(rating);
@@ -44,7 +52,9 @@ const StarRating = _ref => {
       if (isRTL) {
         return calculateRating(width.current - x, false);
       }
-      const newRating = step !== 'full' ? Math.max(0, Math.min(Math.round(x / width.current * maxStars * multiplier + 0.2) / multiplier, maxStars)) : Math.ceil(x / width.current * maxStars);
+      const rawRating = x / width.current * maxStars;
+      const steppedRating = stepValue >= 1 ? Math.ceil(rawRating) : Math.round(rawRating * multiplier) / multiplier;
+      const newRating = Math.max(0, Math.min(steppedRating, maxStars));
       return newRating;
     };
     const handleChange = newRating => {
@@ -113,7 +123,7 @@ const StarRating = _ref => {
       label: accessabilityActivateLabel
     }],
     onAccessibilityAction: event => {
-      const incrementor = step === 'half' ? 0.5 : step === 'quarter' ? 0.25 : 1;
+      const incrementor = stepValue;
       switch (event.nativeEvent.actionName) {
         case 'increment':
           if (stagedRating >= maxStars) {
@@ -136,17 +146,19 @@ const StarRating = _ref => {
           break;
       }
     }
-  }), getStars(rating, maxStars, step).map((starType, i) => {
+  }), getStars(rating, maxStars, step).map((star, i) => {
+    const isEmpty = star.type === 'empty';
     return /*#__PURE__*/React.createElement(AnimatedIcon, {
       key: i,
-      active: isInteracting && rating - i >= 0.5,
+      active: isInteracting && rating - i >= activeThreshold,
       animationConfig: animationConfig,
       style: starStyle
     }, /*#__PURE__*/React.createElement(StarIconComponent, {
       index: i,
-      type: starType,
+      type: star.type,
+      fill: star.fill,
       size: starSize,
-      color: starType === 'empty' ? emptyColor : color
+      color: isEmpty ? emptyColor : color
     }));
   })));
 };
diff --git a/node_modules/react-native-star-rating-widget/lib/module/StarRatingDisplay.js b/node_modules/react-native-star-rating-widget/lib/module/StarRatingDisplay.js
index fa1ce11..19b5b6b 100644
--- a/node_modules/react-native-star-rating-widget/lib/module/StarRatingDisplay.js
+++ b/node_modules/react-native-star-rating-widget/lib/module/StarRatingDisplay.js
@@ -21,15 +21,17 @@ const StarRatingDisplay = _ref => {
     style: [styles.starRating, style],
     accessibilityLabel: accessibilityLabel,
     testID: testID
-  }, getStars(rating, maxStars, step).map((starType, i) => {
+  }, getStars(rating, maxStars, step).map((star, i) => {
+    const isEmpty = star.type === 'empty';
     return /*#__PURE__*/React.createElement(View, {
       key: i,
       style: [styles.star, starStyle]
     }, /*#__PURE__*/React.createElement(StarIconComponent, {
       index: i,
-      type: starType,
+      type: star.type,
+      fill: star.fill,
       size: starSize,
-      color: starType === 'empty' ? emptyColor : color
+      color: isEmpty ? emptyColor : color
     }));
   }));
 };
diff --git a/node_modules/react-native-star-rating-widget/lib/module/utils.js b/node_modules/react-native-star-rating-widget/lib/module/utils.js
index 9043a5f..aadce92 100644
--- a/node_modules/react-native-star-rating-widget/lib/module/utils.js
+++ b/node_modules/react-native-star-rating-widget/lib/module/utils.js
@@ -1,15 +1,39 @@
+const clamp = (value, min, max) => Math.min(max, Math.max(min, value));
 export function getStars(rating, maxStars, step) {
   return [...Array(maxStars)].map((_, i) => {
     const remainder = rating - i;
-    if (remainder >= 1) return 'full';
+    if (remainder >= 1) return {
+      type: 'full'
+    };
+    if (typeof step === 'number') {
+      if (remainder > 0) {
+        return {
+          type: 'fraction',
+          fill: clamp(remainder, 0, 1)
+        };
+      }
+      return {
+        type: 'empty'
+      };
+    }
     if (step === 'quarter') {
-      if (remainder >= 0.75) return 'three-quarter';
-      if (remainder >= 0.5) return 'half';
-      if (remainder >= 0.25) return 'quarter';
+      if (remainder >= 0.75) return {
+        type: 'three-quarter'
+      };
+      if (remainder >= 0.5) return {
+        type: 'half'
+      };
+      if (remainder >= 0.25) return {
+        type: 'quarter'
+      };
     } else if (step === 'half') {
-      if (remainder >= 0.5) return 'half';
+      if (remainder >= 0.5) return {
+        type: 'half'
+      };
     }
-    return 'empty';
+    return {
+      type: 'empty'
+    };
   });
 }
 //# sourceMappingURL=utils.js.map
diff --git a/node_modules/react-native-star-rating-widget/lib/typescript/StarIcon.d.ts b/node_modules/react-native-star-rating-widget/lib/typescript/StarIcon.d.ts
index 6f13378..a2f6965 100644
--- a/node_modules/react-native-star-rating-widget/lib/typescript/StarIcon.d.ts
+++ b/node_modules/react-native-star-rating-widget/lib/typescript/StarIcon.d.ts
@@ -2,8 +2,9 @@ export type StarIconProps = {
     index: number;
     size: number;
     color: string;
-    type: 'full' | 'half' | 'quarter' | 'three-quarter' | 'empty';
+    type: 'full' | 'half' | 'quarter' | 'three-quarter' | 'empty' | 'fraction';
+    fill?: number;
 };
-declare const StarIcon: ({ index, type, size, color }: StarIconProps) => JSX.Element;
+declare const StarIcon: ({ index, type, size, color, fill }: StarIconProps) => JSX.Element;
 export default StarIcon;
 //# sourceMappingURL=StarIcon.d.ts.map
diff --git a/node_modules/react-native-star-rating-widget/lib/typescript/StarRating.d.ts b/node_modules/react-native-star-rating-widget/lib/typescript/StarRating.d.ts
index c3eb879..9c7a3aa 100644
--- a/node_modules/react-native-star-rating-widget/lib/typescript/StarRating.d.ts
+++ b/node_modules/react-native-star-rating-widget/lib/typescript/StarRating.d.ts
@@ -44,7 +44,7 @@ type StarRatingProps = {
      *
      * @default 'half'
      */
-    step?: 'half' | 'quarter' | 'full';
+    step?: 'half' | 'quarter' | 'full' | number;
     /**
      * Enable swiping to rate.
      *
diff --git a/node_modules/react-native-star-rating-widget/lib/typescript/StarRatingDisplay.d.ts b/node_modules/react-native-star-rating-widget/lib/typescript/StarRatingDisplay.d.ts
index de0de56..d126eb8 100644
--- a/node_modules/react-native-star-rating-widget/lib/typescript/StarRatingDisplay.d.ts
+++ b/node_modules/react-native-star-rating-widget/lib/typescript/StarRatingDisplay.d.ts
@@ -48,7 +48,7 @@ type Props = {
      *
      * @default 'half'
      */
-    step?: 'half' | 'quarter' | 'full';
+    step?: 'half' | 'quarter' | 'full' | number;
     /**
      * The accessibility label used on the star component.
      *
diff --git a/node_modules/react-native-star-rating-widget/lib/typescript/utils.d.ts b/node_modules/react-native-star-rating-widget/lib/typescript/utils.d.ts
index 62c70e5..3dfd9c9 100644
--- a/node_modules/react-native-star-rating-widget/lib/typescript/utils.d.ts
+++ b/node_modules/react-native-star-rating-widget/lib/typescript/utils.d.ts
@@ -1,2 +1,7 @@
-export declare function getStars(rating: number, maxStars: number, step?: 'half' | 'quarter' | 'full'): ("full" | "half" | "quarter" | "three-quarter" | "empty")[];
+export type StarType = 'full' | 'half' | 'quarter' | 'three-quarter' | 'empty' | 'fraction';
+export type StarDefinition = {
+    type: StarType;
+    fill?: number;
+};
+export declare function getStars(rating: number, maxStars: number, step?: 'half' | 'quarter' | 'full' | number): StarDefinition[];
 //# sourceMappingURL=utils.d.ts.map
diff --git a/node_modules/react-native-star-rating-widget/src/StarIcon.tsx b/node_modules/react-native-star-rating-widget/src/StarIcon.tsx
index 070548b..d712cd1 100644
--- a/node_modules/react-native-star-rating-widget/src/StarIcon.tsx
+++ b/node_modules/react-native-star-rating-widget/src/StarIcon.tsx
@@ -6,14 +6,15 @@ export type StarIconProps = {
   index: number;
   size: number;
   color: string;
-  type: 'full' | 'half' | 'quarter' | 'three-quarter' | 'empty';
+  type: 'full' | 'half' | 'quarter' | 'three-quarter' | 'empty' | 'fraction';
+  fill?: number;
 };
 
 const RTL_TRANSFORM: ViewStyle = {
   transform: [{ rotateY: '180deg' }],
 };
 
-const StarBorder = ({ size, color }: Omit<StarIconProps, 'type'>) => (
+const StarBorder = ({ size, color }: Omit<StarIconProps, 'type' | 'fill'>) => (
   <Svg height={size} viewBox="0 0 24 24" width={size}>
     <Path
       fill={color}
@@ -22,7 +23,7 @@ const StarBorder = ({ size, color }: Omit<StarIconProps, 'type'>) => (
   </Svg>
 );
 
-const StarFull = ({ size, color, index }: Omit<StarIconProps, 'type'>) => {
+const StarFull = ({ size, color, index }: Omit<StarIconProps, 'type' | 'fill'>) => {
   const gradientId = `full-gradient-${index}`;
   return (
     <Svg height={size} viewBox="0 0 24 24" width={size}>
@@ -42,7 +43,7 @@ const StarFull = ({ size, color, index }: Omit<StarIconProps, 'type'>) => {
   );
 };
 
-const StarQuarter = ({ size, color, index }: Omit<StarIconProps, 'type'>) => {
+const StarQuarter = ({ size, color, index }: Omit<StarIconProps, 'type' | 'fill'>) => {
   const gradientId = `quarter-gradient-${index}`;
   return (
     <Svg height={size} viewBox="0 0 24 24" width={size}>
@@ -62,7 +63,7 @@ const StarQuarter = ({ size, color, index }: Omit<StarIconProps, 'type'>) => {
   );
 };
 
-const StarThreeQuarter = ({ size, color, index }: Omit<StarIconProps, 'type'>) => {
+const StarThreeQuarter = ({ size, color, index }: Omit<StarIconProps, 'type' | 'fill'>) => {
   const gradientId = `three-quarter-gradient-${index}`;
   return (
     <Svg height={size} viewBox="0 0 24 24" width={size}>
@@ -82,7 +83,7 @@ const StarThreeQuarter = ({ size, color, index }: Omit<StarIconProps, 'type'>) =
   );
 };
 
-const StarHalf = ({ size, color, index }: Omit<StarIconProps, 'type'>) => {
+const StarHalf = ({ size, color, index }: Omit<StarIconProps, 'type' | 'fill'>) => {
   const gradientId = `half-gradient-${index}`;
   return (
     <Svg
@@ -107,6 +108,39 @@ const StarHalf = ({ size, color, index }: Omit<StarIconProps, 'type'>) => {
   );
 };
 
+const StarFraction = ({
+  size,
+  color,
+  index,
+  fill = 0,
+}: Omit<StarIconProps, 'type'>) => {
+  const clampedFill = Math.min(1, Math.max(0, fill));
+  const gradientId = `fraction-gradient-${index}`;
+  const offset = `${Math.round(clampedFill * 100)}%`;
+
+  return (
+    <Svg
+      height={size}
+      viewBox="0 0 24 24"
+      width={size}
+      style={I18nManager.isRTL ? RTL_TRANSFORM : undefined}
+    >
+      <Defs>
+        <LinearGradient id={gradientId} x1="0" y1="0" x2="1" y2="0">
+          <Stop offset={offset} stopColor={color} stopOpacity="1" />
+          <Stop offset={offset} stopColor={color} stopOpacity="0" />
+        </LinearGradient>
+      </Defs>
+      <Path
+        d="M12 17.27L18.18 21l-1.64-7.03L22 9.24l-7.19-.61L12 2 9.19 8.63 2 9.24l5.46 4.73L5.82 21z"
+        fill={`url(#${gradientId})`}
+        stroke={color}
+        strokeWidth="1.5"
+      />
+    </Svg>
+  );
+};
+
 const getStarComponent = (type: StarIconProps['type']) => {
   switch (type) {
     case 'full':
@@ -117,14 +151,16 @@ const getStarComponent = (type: StarIconProps['type']) => {
       return StarQuarter;
     case 'three-quarter':
       return StarThreeQuarter;
+    case 'fraction':
+      return StarFraction;
     default:
       return StarBorder;
   }
 };
 
-const StarIcon = ({ index, type, size, color }: StarIconProps) => {
+const StarIcon = ({ index, type, size, color, fill }: StarIconProps) => {
   const Component = getStarComponent(type);
-  return <Component index={index} size={size} color={color} />;
+  return <Component index={index} size={size} color={color} fill={fill} />;
 };
 
 export default StarIcon;
diff --git a/node_modules/react-native-star-rating-widget/src/StarRating.tsx b/node_modules/react-native-star-rating-widget/src/StarRating.tsx
index f757fb1..a78c730 100644
--- a/node_modules/react-native-star-rating-widget/src/StarRating.tsx
+++ b/node_modules/react-native-star-rating-widget/src/StarRating.tsx
@@ -65,7 +65,7 @@ type StarRatingProps = {
    *
    * @default 'half'
    */
-  step?: 'half' | 'quarter' | 'full';
+  step?: 'half' | 'quarter' | 'full' | number;
 
   /**
    * Enable swiping to rate.
@@ -166,6 +166,13 @@ const defaultAnimationConfig: Required<AnimationConfig> = {
   delay: 300,
 };
 
+const getStepValue = (step: StarRatingProps['step']) => {
+  if (typeof step === 'number') return step;
+  if (step === 'quarter') return 0.25;
+  if (step === 'half') return 0.5;
+  return 1;
+};
+
 const StarRating = ({
   rating,
   maxStars = 5,
@@ -188,7 +195,9 @@ const StarRating = ({
   accessabilityActivateLabel = 'activate (default)',
   accessibilityAdjustmentLabel = '%value% stars',
 }: StarRatingProps) => {
-  const multiplier = step === 'quarter' ? 4 : step === 'half' ? 2 : 1;
+  const stepValue = Math.max(0.01, getStepValue(step));
+  const multiplier = 1 / stepValue;
+  const activeThreshold = stepValue;
   const width = React.useRef<number>();
   const [isInteracting, setInteracting] = React.useState(false);
   const [stagedRating, setStagedRating] = React.useState(rating);
@@ -201,17 +210,12 @@ const StarRating = ({
         return calculateRating(width.current - x, false);
       }
 
-      const newRating =
-        step !== 'full'
-          ? Math.max(
-              0,
-              Math.min(
-                Math.round((x / width.current) * maxStars * multiplier + 0.2) /
-                  multiplier,
-                maxStars
-              )
-            )
-          : Math.ceil((x / width.current) * maxStars);
+      const rawRating = (x / width.current) * maxStars;
+      const steppedRating =
+        stepValue >= 1
+          ? Math.ceil(rawRating)
+          : Math.round(rawRating * multiplier) / multiplier;
+      const newRating = Math.max(0, Math.min(steppedRating, maxStars));
 
       return newRating;
     };
@@ -293,8 +297,7 @@ const StarRating = ({
           { name: 'activate', label: accessabilityActivateLabel },
         ]}
         onAccessibilityAction={(event: AccessibilityActionEvent) => {
-          const incrementor =
-            step === 'half' ? 0.5 : step === 'quarter' ? 0.25 : 1;
+          const incrementor = stepValue;
           switch (event.nativeEvent.actionName) {
             case 'increment':
               if (stagedRating >= maxStars) {
@@ -337,19 +340,21 @@ const StarRating = ({
           }
         }}
       >
-        {getStars(rating, maxStars, step).map((starType, i) => {
+        {getStars(rating, maxStars, step).map((star, i) => {
+          const isEmpty = star.type === 'empty';
           return (
             <AnimatedIcon
               key={i}
-              active={isInteracting && rating - i >= 0.5}
+              active={isInteracting && rating - i >= activeThreshold}
               animationConfig={animationConfig}
               style={starStyle}
             >
               <StarIconComponent
                 index={i}
-                type={starType}
+                type={star.type}
+                fill={star.fill}
                 size={starSize}
-                color={starType === 'empty' ? emptyColor : color}
+                color={isEmpty ? emptyColor : color}
               />
             </AnimatedIcon>
           );
diff --git a/node_modules/react-native-star-rating-widget/src/StarRatingDisplay.tsx b/node_modules/react-native-star-rating-widget/src/StarRatingDisplay.tsx
index 734e985..5f97c69 100644
--- a/node_modules/react-native-star-rating-widget/src/StarRatingDisplay.tsx
+++ b/node_modules/react-native-star-rating-widget/src/StarRatingDisplay.tsx
@@ -59,7 +59,7 @@ type Props = {
    *
    * @default 'half'
    */
-  step?: 'half' | 'quarter' | 'full';
+  step?: 'half' | 'quarter' | 'full' | number;
 
   /**
    * The accessibility label used on the star component.
@@ -91,14 +91,16 @@ const StarRatingDisplay = ({
       accessibilityLabel={accessibilityLabel}
       testID={testID}
     >
-      {getStars(rating, maxStars, step).map((starType, i) => {
+      {getStars(rating, maxStars, step).map((star, i) => {
+        const isEmpty = star.type === 'empty';
         return (
           <View key={i} style={[styles.star, starStyle]}>
             <StarIconComponent
               index={i}
-              type={starType}
+              type={star.type}
+              fill={star.fill}
               size={starSize}
-              color={starType === 'empty' ? emptyColor : color}
+              color={isEmpty ? emptyColor : color}
             />
           </View>
         );
diff --git a/node_modules/react-native-star-rating-widget/src/utils.ts b/node_modules/react-native-star-rating-widget/src/utils.ts
index 2b94662..792252c 100644
--- a/node_modules/react-native-star-rating-widget/src/utils.ts
+++ b/node_modules/react-native-star-rating-widget/src/utils.ts
@@ -1,21 +1,45 @@
+export type StarType =
+  | 'full'
+  | 'half'
+  | 'quarter'
+  | 'three-quarter'
+  | 'empty'
+  | 'fraction';
+
+export type StarDefinition = {
+  type: StarType;
+  fill?: number;
+};
+
+const clamp = (value: number, min: number, max: number) =>
+  Math.min(max, Math.max(min, value));
+
 export function getStars(
   rating: number,
   maxStars: number,
-  step?: 'half' | 'quarter' | 'full'
-) {
+  step?: 'half' | 'quarter' | 'full' | number
+): StarDefinition[] {
   return [...Array(maxStars)].map((_, i) => {
     const remainder = rating - i;
 
-    if (remainder >= 1) return 'full';
+    if (remainder >= 1) return { type: 'full' };
+
+    if (typeof step === 'number') {
+      if (remainder > 0) {
+        return { type: 'fraction', fill: clamp(remainder, 0, 1) };
+      }
+
+      return { type: 'empty' };
+    }
 
     if (step === 'quarter') {
-      if (remainder >= 0.75) return 'three-quarter';
-      if (remainder >= 0.5) return 'half';
-      if (remainder >= 0.25) return 'quarter';
+      if (remainder >= 0.75) return { type: 'three-quarter' };
+      if (remainder >= 0.5) return { type: 'half' };
+      if (remainder >= 0.25) return { type: 'quarter' };
     } else if (step === 'half') {
-      if (remainder >= 0.5) return 'half';
+      if (remainder >= 0.5) return { type: 'half' };
     }
 
-    return 'empty';
+    return { type: 'empty' };
   });
 }
